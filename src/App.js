import React, { useState } from "react";
import Subform from "./components/Subform";

const App = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        background: "#000",
      }}
    >
      <Subform />
    </div>
  );
};

export default App;
